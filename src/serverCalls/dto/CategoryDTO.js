class CategoryDTO {
    constructor(id, username, categoryName, parentCategory) {
        this.id = id
        this.username = username
        this.categoryName = categoryName
        this.parentCategory = parentCategory
    }
}

export default CategoryDTO;